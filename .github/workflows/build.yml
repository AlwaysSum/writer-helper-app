# 从 GitCode 拉取 Writer Helper App 源码，构建 Windows / macOS / Android 安装包，
# 上传为 Actions 构建产物；打 tag 推送或勾选发布时，自动发布 GitHub Release（安装包作为附件）。

name: build-release

on:
  workflow_dispatch:
    inputs:
      gitcode_url:
        description: GitCode 源码仓库地址
        required: false
        default: https://gitcode.com/Sunflower816/writer_helper_app.git
      gitcode_branch:
        description: GitCode 源码分支
        required: false
        default: main
      publish:
        description: 构建完成后发布到 GitHub Release
        required: false
        type: boolean
        default: false
  push:
    tags:
      - 'v*'

env:
  GITCODE_URL: ${{ github.event.inputs.gitcode_url || 'https://gitcode.com/Sunflower816/writer_helper_app.git' }}
  GITCODE_BRANCH: ${{ github.event.inputs.gitcode_branch || 'main' }}

concurrency:
  group: build-release
  cancel-in-progress: true

jobs:
  # 把 GitHub 发布仓库自身（含本 workflow）保留在工作区，并把源码克隆到 ./src
  prepare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Clone source from GitCode
        shell: bash
        run: |
          if [ -n "${{ secrets.GITCODE_TOKEN }}" ]; then
            AUTH_URL=$(printf '%s' "$GITCODE_URL" | sed "s#://#://Sunflower816:${{ secrets.GITCODE_TOKEN }}@#")
          else
            AUTH_URL="$GITCODE_URL"
          fi
          git clone --depth 1 --branch "$GITCODE_BRANCH" "$AUTH_URL" src
      - name: Upload source workspace
        uses: actions/upload-artifact@v4
        with:
          name: source-workspace
          path: .
          include-hidden-files: true

  # ------------------------------------------------------------------
  # Windows：flutter build windows --release + Inno Setup 制作安装包
  # ------------------------------------------------------------------
  windows:
    needs: prepare
    runs-on: windows-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: source-workspace
          path: .
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install Inno Setup (for installer)
        shell: pwsh
        run: choco install innosetup -y --no-progress

      - name: Resolve version
        id: version
        shell: pwsh
        working-directory: src
        run: |
          $m = select-string -path pubspec.yaml -pattern '^version:\s*(\S+)'
          "version=$($m.Matches[0].Groups[1].Value)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      - name: Flutter build windows
        shell: pwsh
        working-directory: src
        run: flutter build windows --release

      - name: Create installer with Inno Setup
        shell: pwsh
        working-directory: src
        run: |
          $ver = '${{ steps.version.outputs.version }}'
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) { $iscc = "${env:ProgramFiles}\Inno Setup 6\ISCC.exe" }
          & $iscc windows\installer.iss "/DMyAppVersion=$ver" "/DMyAppExeName=WriterHelper.exe" /Qp

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: writer-helper-windows
          path: src/build/windows/pkg/WriterHelperSetup-*.exe

  # ------------------------------------------------------------------
  # macOS：flutter build macos --release，关闭强签名校验，产物压缩为 zip
  # ------------------------------------------------------------------
  macos:
    needs: prepare
    runs-on: macos-latest
    env:
      CODE_SIGNING_ALLOWED: 'NO'
      CODE_SIGNING_REQUIRED: 'NO'
      CODE_SIGN_IDENTITY: ''
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: source-workspace
          path: .
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Resolve version
        id: version
        working-directory: src
        run: |
          VER=$(awk -F': *' '/^version:/{print $2; exit}' pubspec.yaml)
          echo "version=$VER" >> "$GITHUB_OUTPUT"

      - name: Flutter build macos
        working-directory: src
        run: flutter build macos --release

      - name: Normalize Mpv.framework symlink
        working-directory: src
        run: |
          cd "build/macos/Build/Products/Release/"*.app/Contents/Frameworks/Mpv.framework
          # media_kit_video 链接装载 Versions/A/Mpv，Dart 又 dlopen 顶层 Mpv；二者必须命中同一份
          # libmpv。若顶层 Mpv 被 CocoaPods/zip 解引用成真实文件，会双份加载并复注册 m_config_cache，
          # mpv_render_context_create 断言 SIGABRT。此处强制恢复符号链接，使两份路径归一到同一 inode。
          if [ -f Mpv ] && [ ! -L Mpv ]; then rm -f Mpv; fi
          ln -sfn Versions/A/Mpv Mpv
          ls -la

      - name: Package app bundle
        working-directory: src
        run: |
          cd build/macos/Build/Products/Release
          # -y/--symlinks: 以符号链接形式入库，避免把框架内的符号链接解引用成真实文件
          # （否则同 inode 缺失，Dart dlopen 到第二份 libmpv 触发 m_config_cache 崩溃）
          zip -rqy writer-helper-macos-${{ steps.version.outputs.version }}.zip *.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: writer-helper-macos
          path: src/build/macos/Build/Products/Release/writer-helper-macos-*.zip

  # ------------------------------------------------------------------
  # Linux：按需求移除 Linux 安装包构建（不再发布该平台）
  # ------------------------------------------------------------------

  # ------------------------------------------------------------------
  # Android：flutter build apk --release（当前 release 使用 debug 签名）
  # ------------------------------------------------------------------
  android:
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: source-workspace
          path: .
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Resolve version
        id: version
        working-directory: src
        run: |
          VER=$(awk -F': *' '/^version:/{print $2; exit}' pubspec.yaml)
          echo "version=$VER" >> "$GITHUB_OUTPUT"

      - name: Flutter build apk
        working-directory: src
        run: flutter build apk --release

      - name: Rename apk
        working-directory: src
        run: |
          mv build/app/outputs/flutter-apk/app-release.apk \
             writer-helper-android-${{ steps.version.outputs.version }}.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: writer-helper-android
          path: src/writer-helper-android-*.apk

  # ------------------------------------------------------------------
  # 发布：合并三平台安装包并发布 GitHub Release（勾选 publish 或打 tag 时执行）
  # ------------------------------------------------------------------
  release:
    needs: [windows, macos, android]
    runs-on: ubuntu-latest
    if: |
      startsWith(github.ref, 'refs/tags/') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')
    permissions:
      contents: write
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: writer-helper-*
          merge-multiple: true

      - name: Download source workspace for version resolution
        uses: actions/download-artifact@v4
        with:
          name: source-workspace
          path: .

      - name: Determine tag name
        id: tag
        working-directory: src
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "name=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          else
            VER=$(awk -F': *' '/^version:/{print $2; exit}' pubspec.yaml || true)
            echo "name=v${VER:-0.0.0}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          generate_release_notes: true
          draft: false
          prerelease: false
          files: artifacts/*

      - name: List artifacts
        if: always()
        run: ls -lhR artifacts